# Cloudflare Pages Migration Configuration
# API calls use same-origin (Cloudflare Pages Functions)
VITE_API_URL=

# AWS Cognito (unchanged)
VITE_USER_POOL_ID=
VITE_USER_POOL_CLIENT_ID=
VITE_REGION=ap-northeast-1

# R2 public domain (replace after R2 bucket setup)
# Format: https://pub-{hash}.r2.dev or custom domain
VITE_R2_DOMAIN=
