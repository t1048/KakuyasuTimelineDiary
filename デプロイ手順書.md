# 📘 激安日記アプリ デプロイ手順書（Cloudflare版）

この手順書は、**Cloudflare Pages (フロントエンド/API)** と **AWS Cognito (認証)** を組み合わせた、最新の「激安」構成でのデプロイガイドです。

## ✅ 前提条件

- **AWSアカウント**: 認証機能（Cognito）のみに使用します。
- **Cloudflareアカウント**: アプリのホスティング、データベース、画像保存に使用します（無料プラン可）。
- **ローカル環境**:
  - Node.js (v18以上)
  - Python (3.11推奨)
  - AWS CLI / CDK
  - Wrangler (`npm install -g wrangler` または `npx` で実行)

---

## 🏗️ Step 1: 認証基盤 (AWS Cognito) のデプロイ

まず、認証機能を提供する AWS Cognito をデプロイします。

### 1-1. CDKの準備

```bash
cd gekiyasu_diary_cdk_py

# 仮想環境の作成と有効化（Windows）
python -m venv .venv
.venv\Scripts\activate

# 依存関係のインストール
pip install -r requirements.txt
```

### 1-2. デプロイ実行

```bash
# 初回のみBootstrapが必要（既に実行済みの場合は不要）
cdk bootstrap

# デプロイ
cdk deploy
```

### 1-3. 情報の取得

デプロイ完了後、ターミナルに表示される **Outputs** を必ずメモしてください。後で使用します。

```text
KakuyasuTimelineDiaryStack.UserPoolId = ap-northeast-1_xxxxxxxxx
KakuyasuTimelineDiaryStack.UserPoolClientId = xxxxxxxxxxxxxxxxxxxxxxxxxx
```

### 1-4. ユーザーの作成

本システムはセルフサインアップを無効化しています。AWSコンソールの Cognito 画面から、最初のユーザーを作成してください。

1. AWSコンソールで **Amazon Cognito** を開く。
2. 作成されたユーザープール（`kakuyasu-timeline-diary-user-pool`）を選択。
3. 「ユーザー」タブで「ユーザーを作成」をクリック。
4. メールアドレス等を設定して作成。

---

## ☁️ Step 2: アプリ基盤 (Cloudflare) のデプロイ

次に、フロントエンド、API、データベース、ストレージを Cloudflare 上に構築します。

### 2-1. セットアップ

```bash
cd ../web-app

# 依存関係のインストール
npm install

# Cloudflareへログイン
npx wrangler login
```

### 2-2. リソースの作成 (D1 & R2)

データベース (D1) と ストレージ (R2) を作成します。

```bash
# D1データベース作成
npx wrangler d1 create kakuyasu-timeline-diary-db

# R2バケット作成
npx wrangler r2 bucket create kakuyasu-timeline-user-content
```

`d1 create` コマンドを実行すると、以下のような出力が表示されます。この `database_id` をメモしてください。

```text
[[d1_databases]]
binding = "DB"
database_name = "kakuyasu-timeline-diary-db"
database_id = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
```

### 2-3. 設定ファイルの更新 (wrangler.toml)

1. `web-app` ディレクトリにある `wrangler.toml.example` を `wrangler.toml` にリネーム（またはコピー）します。
2. `wrangler.toml` をエディタで開き、以下の項目を書き換えます。

```toml
# ... (略) ...

[[d1_databases]]
binding = "DB"
database_name = "kakuyasu-timeline-diary-db"
database_id = "ここにStep 2-2で取得したdatabase_idを貼り付け" # 📝 変更

[[r2_buckets]]
binding = "USER_CONTENT_BUCKET"
bucket_name = "kakuyasu-timeline-user-content"

[vars]
AWS_REGION = "ap-northeast-1"
USER_POOL_ID = "ここにStep 1-3で取得したUserPoolId" # 📝 変更
USER_POOL_CLIENT_ID = "ここにStep 1-3で取得したUserPoolClientId" # 📝 変更
# ... (他はそのまま) ...
```

### 2-4. 環境変数の設定 (.env)

フロントエンド用の環境変数を設定します。

1. `.env.example` を `.env` にリネーム（またはコピー）します。
2. 内容を編集します。

```properties
VITE_API_URL=
# Cloudflare Pages Functionsを利用するため、VITE_API_URLは空欄のままでOKです（自動的に同一オリジンになります）

VITE_USER_POOL_ID=ここにStep 1-3で取得したUserPoolId
VITE_USER_POOL_CLIENT_ID=ここにStep 1-3で取得したUserPoolClientId
VITE_REGION=ap-northeast-1
VITE_R2_DOMAIN=
# R2の公開URLを使用しない場合（推奨）は空欄でOK
```

### 2-5. データベースの初期化

テーブル定義をデータベースに適用します。

```bash
# ローカル用（開発時のみ）
npx wrangler d1 execute kakuyasu-timeline-diary-db --local --file=./migrations/0001_initial_schema.sql

# 本番用（デプロイ前必須）
npx wrangler d1 execute kakuyasu-timeline-diary-db --remote --file=./migrations/0001_initial_schema.sql
```

---

## 🚀 Step 3: ビルドとデプロイ

### 3-1. ローカルでの動作確認 (推奨)

```bash
# ビルド
npm run build

# ローカルサーバー起動 (D1/R2もエミュレートされます)
npx wrangler pages dev dist
```

ブラウザで `http://localhost:8788` にアクセスし、ログインや投稿ができるか確認します。

### 3-2. 本番デプロイ

```bash
# ビルド
npm run build

# Cloudflare Pagesへデプロイ
npx wrangler pages deploy dist --project-name=kakuyasu-timeline-diary
```

初めてのデプロイ時は、プロジェクトを作成するか聞かれる場合があります。「Create a new project」を選択し、名前（`kakuyasu-timeline-diary`）を確認して進めてください。

デプロイが完了すると、発行されたURLが表示されます（例: `https://kakuyasu-timeline-diary.pages.dev`）。

---

## 🔄 変更時のデプロイ手順

### フロントエンド / API の変更

コードを修正したら、以下を実行するだけです。

```bash
cd web-app
npm run build
npx wrangler pages deploy dist --project-name=kakuyasu-timeline-diary
```

### データベース定義の変更

新しいマイグレーションファイル（例: `migrations/0002_add_column.sql`）を作成し、以下を実行します。

```bash
npx wrangler d1 execute kakuyasu-timeline-diary-db --remote --file=./migrations/0002_add_column.sql
```

### 認証設定 (Cognito) の変更

CDKコードを修正し、以下を実行します。

```bash
cd gekiyasu_diary_cdk_py
cdk deploy
```